Run unittests:
$ python -m unittest discover

Run unittests with coverage (requires coverage.py):
$ coverage run -m unittest discover

Coverage report:
$ coverage report

Coverage to html:
$ coverage html